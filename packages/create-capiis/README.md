# create-capiis

Scaffold a new Capiis workspace for Claude Code or OpenCode.

## Usage

```bash
npx create-capiis my-wealth
cd my-wealth
claude      # or: opencode
```

Then run:

```text
/capiis
```

The scaffold clones the latest Capiis repository, removes the original `.git`
history, and installs npm dependencies.
